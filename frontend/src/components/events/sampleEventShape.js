/**
 * Example record shape for API / fixtures (not used by the live UI).
 *
 * @example
 * const events = [
 *   {
 *     time: '14:02:52',
 *     severity: 'critical',
 *     layer: 'api',
 *     event: 'bulk_read',
 *     service: 'payment',
 *     src: '10.0.2.4',
 *     target: '/accounts/*',
 *     mitre: 'T1041',
 *   },
 * ];
 */

export const sampleEvents = [];
