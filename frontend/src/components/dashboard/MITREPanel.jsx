import MitrePanel from '@/components/charts/MitrePanel';

/**
 * Dashboard entry — delegates to charts MITRE matrix (fetches mapping).
 */
export default function MITREPanel() {
  return <MitrePanel />;
}
