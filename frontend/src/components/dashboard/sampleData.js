/**
 * Example shapes for backend integration tests — not imported by the live UI.
 * Replace with API responses in `AuthenticatedApp` / data hooks.
 */

export const sampleIncidents = [];

export const sampleEvents = [];

export const sampleRiskRows = [];
